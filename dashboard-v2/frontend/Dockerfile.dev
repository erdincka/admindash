FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source (will be synced by Tilt)
COPY . .

# Expose port
EXPOSE 3000

# Development server with hot reload
CMD ["npm", "run", "dev"]
